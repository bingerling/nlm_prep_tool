# Core dependencies for nlm_prep_tool
# Install with: pip install -r requirements.txt

# For DOCX file support (optional but recommended)
python-docx>=0.8.11

# For PDF file support (optional but recommended)
PyMuPDF>=1.23.0
